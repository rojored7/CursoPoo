class UberPool extends Car{
    String brand;
    String model;

    public UberPool(String license, Account driver) {
        super(license, driver);
        this.brand = brand;
        this.model = model;

    }
}